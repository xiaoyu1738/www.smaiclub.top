{
  "name": "chat-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node --check src/index.js && node --check src/ChatRoom.js && node --check src/utils.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
